export const environment = {
  production: true,
  supabaseUrl: 'YOUR_SUPABASE_URL',
  supabaseKey: 'YOUR_SUPABASE_ANON_KEY',
  // MUST be the publicly reachable HTTPS URL where email-server is deployed
  emailServerUrl: 'https://unboxd-join-waitlist.vercel.app'
};